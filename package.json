{
  "name": "fifa-filter",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:icons": "node scripts/build-icons.js",
    "build:sprite": "node scripts/build-sprite.js",
    "optimize:logos": "node scripts/optimize-logos.js",
    "build": "npm run build:icons && npm run build:sprite && vite build"
  },
  "devDependencies": {
    "sharp": "^0.35.1",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35"
  }
}
